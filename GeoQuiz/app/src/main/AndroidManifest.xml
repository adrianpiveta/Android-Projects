<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alura.geoquiz">

    <application
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GeoQuiz">
        <!--Criação automática por parte do wizard-->
        <!--activity
            <-.CheatActivity é a mesma coisa de escrever o nome completo com.a.b.b.c.v
                .CheatActivity
            android:name=".CheatActivity"
            android:exported="false" /-->
        <!--Exported diz a respeito de poder iniciar ou não a aplicação, quando é falso, somente
        a própria aplicação pode lança-lo-->
        <activity android:name=".CheatActivity">
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>